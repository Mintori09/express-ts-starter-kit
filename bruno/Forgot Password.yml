info:
  name: Forgot Password
  type: http
  seq: 5

http:
  method: POST
  url: http://localhost:4000/api/v1/password/forgot-password
  body:
    type: json
    data: |-
      {
                "email": "use3r@example.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
