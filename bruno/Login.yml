info:
  name: Login
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:4000/api/v1/auth/login
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
